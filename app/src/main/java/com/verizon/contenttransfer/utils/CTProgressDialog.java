package com.verizon.contenttransfer.utils;

/**
 * Created by rahiahm on 9/7/2016.
 */
public class CTProgressDialog {

/*    private static final String TAG = CTProgressDialog.class.getName();
    private static CTProgressDialog instance;

    public static CTProgressDialog getInstance() {
        if (instance == null) {
            //LogUtil.d(TAG,"Initializing CTGlobal. P2PStartupActivity.contentTransferContext="+ P2PStartupActivity.contentTransferContext);
            instance = new CTProgressDialog();
        }
        return instance;
    }

    private ProgressDialog pDialog = null;

    public void showDialog(Activity activity, String msg){
        pDialog = new ProgressDialog(activity);
        pDialog.setMessage(msg);
        pDialog.show();
    }

    public void showDialog(Activity activity, String msg, boolean cancellable){
        pDialog = new ProgressDialog(activity);
        pDialog.setMessage(msg);
        pDialog.setCanceledOnTouchOutside(cancellable);
        pDialog.show();
    }

    public void showDialog(Activity activity, String msg, boolean cancelButton, String cancelText){
        pDialog = new ProgressDialog(activity);
        pDialog.setMessage(msg);
        pDialog.setCanceledOnTouchOutside(false);

        if(cancelButton) {
            pDialog.setButton(DialogInterface.BUTTON_NEGATIVE, cancelText, new DialogInterface.OnClickListener() {
                @Override
                public void onClick(DialogInterface dialog, int which) {
                    dialog.dismiss();
                }
            });
        }
        pDialog.show();
    }

    public void dismissDialog() {
        if (pDialog != null && pDialog.isShowing()) {
            pDialog.dismiss();
            pDialog = null;
        }
    }*/
}
